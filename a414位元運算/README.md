題目有說測資可能比較多，所以加上ios::sync_with_stdio(false);以及cin.tie(0);


想法是因為輸入一個數n，看n+1的二進位需要進位幾次，所以只要把n拆解成二進位，
存目前的餘數，如果是1，則到時候n+1會需要進位一次，同理，下一位數也是一樣，
故建立vector，只要是1就push_back進去，一遇到餘數為0就break，然後輸出直接用vector.size()就可以了。

輸入0結束則在while判斷cin加上&&，&&只要非0都是可以繼續。
